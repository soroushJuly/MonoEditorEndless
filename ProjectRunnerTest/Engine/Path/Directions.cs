namespace MonoEditorEndless.Engine.Path
{
    // Path directions
    enum Directions
    {
        // First enum for iterating enum, Z, X, -Z, -X, Last enum for iterating enum
        FIRST, NORTH, EAST, SOUTH, WEST, LAST
    }
}
